= form_for @private_chat_room do |f|
  - if @private_chat_room.errors.any?
    #error_explanation
      %h2= "#{pluralize(@private_chat_room.errors.count, "error")} prohibited this private_chat_room from being saved:"
      %ul
        - @private_chat_room.errors.full_messages.each do |message|
          %li= message

  .actions
    = f.submit 'Save'
